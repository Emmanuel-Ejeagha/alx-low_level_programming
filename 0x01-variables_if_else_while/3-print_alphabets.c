#include <stdio.h>
/**
 * main - print lowercase alphabet
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char letter;

	for (letter = 'a'; letter <= 'z'; letter++)

	for (letter = 'A'; letter <= 'Z'; letter++)

		putchar(letter);

	putchar('\n');

	return (0);
}
