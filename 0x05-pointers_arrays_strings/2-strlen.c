#include "main.h"
/**
 * _strlen - the function that returns the length of a string
 * @s: the char
 * Description: returns the length of a string
 *
 * Return: Always 0
*/
int _strlen(char *s)
{
	int i = 0;

	for (; *i++;)
		i++;
	return (i);
}
