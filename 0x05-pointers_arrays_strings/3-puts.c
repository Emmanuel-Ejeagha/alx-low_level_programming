#include "main.h"
/**
 * _puts: this function prints a string followed by a new line
 * @str
 *
 *Return: Always 0
*/
void _puts(char *str)
{
	int x = 0;

	while (str[n] != '\0')
	{
		_putchar(str[n]);
		i++;
	}
	_putchar('\n');
}
